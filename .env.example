# Docker Compose host ports only.
# Copy to .env in the project root to override local container ports.
# Keep backend/.env and frontend/.env for application settings.

FRONTEND_PORT=8137
BACKEND_PORT=8138
POSTGRES_PORT=8139
MAILPIT_SMTP_PORT=8140
MAILPIT_UI_PORT=8141
